# Runtime-only dependencies for the Azure container.
# Dev tools (black, isort, flake8, mypy, pytest*) and local inference packages
# (onnx, onnxruntime, optimum) are intentionally excluded to keep the image small.
# All model inference runs on Tinker's remote GPUs — torch is not needed at runtime.

# Tinker (remote GPU training + inference)
tinker>=0.9.0
tinker-cookbook>=0.2.0

# Transformers (tokenizer only — no model weights downloaded in container)
transformers==4.57.6
huggingface_hub>=0.23.0

# W&B logging
wandb>=0.16.6

# Data & Processing
datasets==2.21.0
pandas==2.2.0
numpy==1.26.4
scikit-learn==1.4.2
tqdm==4.66.4

# Config & Utilities
python-dotenv==1.0.0
pyyaml==6.0.1

# Evaluation metrics
rouge-score==0.1.2
nltk==3.8.1
evaluate==0.4.1

# Logging
structlog==24.1.0
